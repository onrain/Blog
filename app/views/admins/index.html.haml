%h1 Listing admins

%table
  %tr
    %th Title
    %th Published
    %th Data create
    %th Article description
    %th Article content
    %th
    %th
    %th

  - @admins.each do |admin|
    %tr
      %td= image_tag admin.image.url(:small)
      %td= admin.title
      %td= admin.published
      %td= admin.data_create
      %td= admin.article_description
      %td= admin.article_content
      %td= link_to 'Show', admin
      %td= link_to 'Edit', edit_admin_path(admin)
      %td= link_to 'Destroy', admin, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Admin', new_admin_path
